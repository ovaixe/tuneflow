/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "njozvqorenekeyqwutyi.supabase.co" },
    ],
  },
};

export default nextConfig;
